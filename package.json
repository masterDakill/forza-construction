{
  "devDependencies": {
    "@wix/cli": "^1.0.0",
    "@wix/eslint-plugin-cli": "^1.0.0",
    "eslint": "^8.25.0",
    "react": "16.14.0"
  },
  "scripts": {
    "dev": "wix dev",
    "lint": "eslint .",
    "sync": "git pull && git add . && git commit -m 'Auto-sync' && git push",
    "deploy": "wix publish --yes",
    "optimize": "npm run lint && git status"
  }
}